package io.flutter.plugins;

import io.flutter.embedding.engine.FlutterEngine;

/**
 * Stub implementation to prevent Flutter plugin registration build issues.
 * When building the full app with proper Flutter module integration, the real
 * plugins are registered through the Flutter embedding.
 * This stub prevents "package does not exist" errors during compilation.
 */
public class GeneratedPluginRegistrant {
    public static void registerWith(FlutterEngine flutterEngine) {
        // Stub implementation - no actual plugin registration needed here.
        // The real plugins are registered through the Flutter module integration.
    }
}
