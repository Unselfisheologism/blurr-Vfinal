# BMM Workflow Status - Twent (Panda)
# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-12-10T19:44:47+05:30"
project: "Twent (Panda)"
project_type: "Android Mobile Application"
selected_track: "bmad-method"
field_type: "brownfield"
workflow_path: ".bmad/bmm/workflows/workflow-status/paths/method-brownfield.yaml"

workflow_status:
  # ─────────────────────────────────────────────────────────────
  # PREREQUISITE: Documentation (for brownfield without docs)
  # ─────────────────────────────────────────────────────────────
  prerequisite:
    document-project:
      status: "docs/index.md"  # COMPLETED
      agent: "analyst"
      output: "Comprehensive project documentation"
      completed_at: "2025-12-10T19:36:00+05:30"

  # ─────────────────────────────────────────────────────────────
  # PHASE 0: Discovery (Optional)
  # ─────────────────────────────────────────────────────────────
  phase_0_discovery:
    brainstorm-project:
      status: "optional"
      agent: "analyst"
      note: "User may run if creative exploration needed"
    
    research:
      status: "optional"
      agent: "analyst"
      note: "User may run for domain/technical analysis"
    
    product-brief:
      status: "optional"
      agent: "analyst"
      note: "Less common for brownfield, user may skip"

  # ─────────────────────────────────────────────────────────────
  # PHASE 1: Planning
  # ─────────────────────────────────────────────────────────────
  phase_1_planning:
    prd:
      status: "docs/prd.md"  # COMPLETED
      agent: "pm"
      output: "PRD focused on new features/changes"
      completed_at: "2025-12-10T21:00:00+05:30"
    
    create-ux-design:
      status: "conditional"
      condition: "if_has_ui"
      agent: "ux-designer"

  # ─────────────────────────────────────────────────────────────
  # PHASE 2: Solutioning
  # ─────────────────────────────────────────────────────────────
  phase_2_solutioning:
    create-architecture:
      status: "docs/architecture.md"  # COMPLETED
      agent: "architect"
      output: "Integration architecture - solution design"
      completed_at: "2025-12-10T21:17:00+05:30"
    
    create-epics-and-stories:
      status: "docs/epics.md"  # COMPLETED
      agent: "pm"
      note: "Break down PRD into implementable epics and stories"
      completed_at: "2025-12-10T21:40:00+05:30"
    
    test-design:
      status: "recommended"
      agent: "tea"
      output: "System-level testability review"
    
    validate-architecture:
      status: "optional"
      agent: "architect"
    
    implementation-readiness:
      status: "required"
      agent: "architect"
      note: "Gate check: validates PRD + Architecture + Epics + UX"

  # ─────────────────────────────────────────────────────────────
  # PHASE 3: Implementation
  # ─────────────────────────────────────────────────────────────
  phase_3_implementation:
    sprint-planning:
      status: "required"
      agent: "sm"
      note: "Creates sprint plan with stories - tracked in sprint-status.yaml"

# ─────────────────────────────────────────────────────────────
# CURRENT STATE
# ─────────────────────────────────────────────────────────────
current_phase: 3
next_workflow: "code-review"
next_agent: "dev"
next_command: "/bmad-bmm-workflows-code-review"
